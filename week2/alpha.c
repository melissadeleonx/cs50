#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main (void)
{ string word;
    // Prompt the user to put lowercase letters from a-f)
        word = get_string("Please type from a-f: ");
        if(word = isalpha)
        {
            printf("Yes");
            }
        else
        {
            printf("No");
            }
}