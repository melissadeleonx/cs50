#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main (void)
{ string word;
    // Prompt the user to put lowercase letters from a-f)
        word = get_string("Please type from a-f: ");
        if(){
            printf("Yes");
        }

        else
        printf("No");
}