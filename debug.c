#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x = 
    int y =
    int result =



    if (x || y != 0) {
        result = num1 / num2;
        printf("The result of %d and %d is: %.d\n", num1, num2, result);
    } else {
        printf("Division by zero is not allowed.\n");
    }

    return 0;
}