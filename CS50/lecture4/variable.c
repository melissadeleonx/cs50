#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Interchangeably, we can use char *s as string is really just the pointer for char data types
    string s = "HI!";
    printf("%s\n", s);
}