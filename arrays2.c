#include <cs50.h>
#include <stdio.h>

int main (void)
{
    int n[5];
    do
    {
        n = get_int("Size: ");

        for(int i = 1; i < 5; i++)
    {
        printf("%i\n", n[i]);
    }
}
    }
    while (size < 0);

