#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    string plaintext = get_string("plaintext: ");
    for (i = argv[1]; i < strlen(plaintext); i++)
    {
        
    }

    printf("ciphertext: %s\n", plaintext);

}
