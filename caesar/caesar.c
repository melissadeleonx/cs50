#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    string plaintext = get_string("plaintext: ");
    for (int i = 0; i < argc; i++)
    {
         printf("ciphertext: %s\n", plaintext);
    }



}
